# Mandrel Root Environment Configuration
# ========================================
# Copy this file to .env: cp .env.example .env
# These defaults work with docker-compose out of the box!

# ===========================================
# ENVIRONMENT
# ===========================================
NODE_ENV=development

# ===========================================
# DATABASE (matches docker-compose defaults)
# ===========================================
# Use MANDREL_ prefix (preferred) or DATABASE_ (legacy)
DATABASE_USER=mandrel
DATABASE_HOST=localhost
DATABASE_NAME=aidis_production
DATABASE_PASSWORD=mandrel_dev_password
DATABASE_PORT=5432

# Or use MANDREL_ prefix:
# MANDREL_DATABASE_USER=mandrel
# MANDREL_DATABASE_HOST=localhost
# MANDREL_DATABASE_NAME=aidis_production
# MANDREL_DATABASE_PASSWORD=mandrel_dev_password
# MANDREL_DATABASE_PORT=5432

# ===========================================
# REDIS (for BullMQ job queues)
# ===========================================
REDIS_URL=redis://localhost:6379

# ===========================================
# OPTIONAL: FOR DEVELOPMENT WITHOUT DOCKER
# ===========================================
# If running PostgreSQL natively (e.g., via Homebrew on macOS):
# DATABASE_USER=your_username
# DATABASE_PASSWORD=your_password
# DATABASE_NAME=aidis_development

# ===========================================
# LOGGING (optional)
# ===========================================
LOG_LEVEL=info
DB_LOG_LEVEL=warn

# ===========================================
# NOTES
# ===========================================
# The database name is 'aidis_production' by convention.
# This is the canonical database name used across all environments.
#
# For native macOS/Linux setup without Docker:
# 1. Install PostgreSQL with pgvector extension
# 2. Create the database: createdb aidis_production
# 3. Enable pgvector: psql -d aidis_production -c "CREATE EXTENSION vector;"
# 4. Update DATABASE_USER and DATABASE_PASSWORD above
