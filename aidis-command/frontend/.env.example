# AIDIS Command Frontend Environment Variables
# Copy this file to .env and configure as needed

# MCP Server Base URL (root URL only, paths are added by clients)
# Default: http://localhost:8080 (development)
# Production: Set to your MCP server URL (e.g., http://mcp-server:8080)
VITE_MCP_BASE_URL=http://localhost:8080

# Backend API URL (aidis-command backend)
# Default: http://localhost:5001 (development)
# Production: Set to your backend URL (e.g., http://backend:5001)
VITE_API_BASE_URL=http://localhost:5001
